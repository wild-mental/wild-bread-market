# DECISION_LOG — 가이드·발표자료에 없는 결정

기준 자료: `docs/guides/MAKJI_Cafe24_MVP_교육가이드.html`(연동 레퍼런스 40개 파일), 발표 덱 `index.html` 3~6장,
`references/mobile-screens/U01~U06`, `references/ux-intro-slides/01~02`, `docs/DESIGN.md`.

분류
- **CORE**: 아키텍처·보안·데이터 구조·가이드 계약(라우트 경로·응답 JSON·오류 코드·결과 코드·테이블 이름) 변경
- **MINOR**: 화면 문구·레이아웃·목업 값 세부

카운터 (grep용, 항목을 추가하면 같이 올린다)

CORE: 0
MINOR: 8

가이드의 **실습 코드 40개 파일** 중 39개는 가이드 코드 박스와 바이트 단위로 같다. 바꾼 파일은 가이드가 "전체 교체" 대상으로 둔 `src/app/page.tsx` 하나다(MINOR-01). 코드가 아닌 검증 기록표 `docs/live-test-report.md`도 가이드 내용 그대로 두었다. 라우트 경로·응답 JSON·오류 코드·결과 코드·테이블 이름은 바꾸지 않았다.

---

## MINOR-01 · `/`를 실습용 랜딩 대신 MVP 랜딩으로 교체
- 결정: 가이드의 `src/app/page.tsx`(지수 카드 3개 + 상품 링크) 자리에 이번 주 챌린지 안내가 있는 MVP 랜딩을 둔다. 통밀 카드에는 `storefrontProductUrl()` 링크(자사몰 상품 16)를 그대로 남긴다. 하단에 `/admin/cafe24` 링크와 홈·예측·결과 탭바를 추가했다.
- 근거: 가이드 파일 주석이 "기존 MVP가 있으면 이 파일 대신 MVP의 랜딩을 사용한다"고 안내한다. 위젯 CTA(`/?utm_source=cafe24…`)가 도착하는 곳도 이 랜딩이다.
- 영향: 가이드 8-1(지수 카드 → 상품 상세 링크)은 같은 함수로 동작한다.

## MINOR-02 · 체험 시계를 고정 시각에서 시작
- 결정: 목업 시계는 2026-09-17(목) 06:24:50 KST에서 시작해 실제 흐른 초만큼 간다. 회차 마감은 영업일 09:00 KST로 두어 카운트다운이 U04 화면과 같은 02:35:10에서 시작한다. 마감 규칙을 바로 볼 수 있게 `/predict`에 "체험 도구(마감 10초 전으로 · 시계 처음으로 · 저장한 예측 지우기)"를 둔다.
- 근거: 발표자료 7장은 "시각은 협의 후 확정"이라 실제 시각이 없다. 고정 시각이어야 화면·테스트·smoke가 항상 같은 값을 본다.
- 확인 필요: HUMAN_TODO TODO-25.

## MINOR-03 · 등락 색은 DESIGN.md 의미색을 따른다
- 결정: 상승 ▲은 `--up`(#2E9E5B 그린), 하락 ▼은 `--down`(#E0463F 레드), 적중은 그린 칩, 미적중은 회색 칩.
- 근거: 모바일 화면 원본(U02)은 빨강 ▲ · 파랑 ▼이지만 `docs/DESIGN.md`가 SSOT이고 덱 2장 지수 카드도 그린/레드다.

## MINOR-04 · 골든 브레드 결과 행을 지수 값과 맞춤
- 결정: `/result`의 골든 브레드 행은 "내 예측 DOWN · 실제 UP · 미적중"으로 표시한다(U06 원본은 "내 예측 UP · 실제 DOWN").
- 근거: 같은 목업의 골든 브레드 지수가 105.1 ▲0.8%라 실제 방향은 UP이어야 한다. "3개 중 2개 적중"은 그대로 유지된다.

## MINOR-05 · 제품 사진 대신 이모지 썸네일
- 결정: 지수 카드 썸네일은 🍞(통밀) · 🥐(크루아상) · 🥯(골든)를 옅은 블루 칸에 넣는다. 로고는 `assets/brand/makji-logo.png`를 360px로 줄인 사본(`public/brand/makji-logo.png`)을 쓴다.
- 근거: 저장소에 개별 빵 사진 원본이 없고, 목업 PNG를 잘라 쓰면 DESIGN.md 5-1의 크롭 금지 규칙과 충돌한다.

## MINOR-06 · 목업 값 세부
- 결정: 참여 인원 1,284명. 확정값 시계열 09-11/14/15/16 = 통밀 100.6/101.5/101.2/102.4 · 크루아상 98.9/99.4/99.2/98.7 · 골든 103.6/104.0/104.3/105.1. 지난 예측 월·화·수 각 3개 중 2개 적중(주간 9개 중 6개, 66.7%). 스파크라인은 그 앞 7개 값을 덧붙여 그린다.
- 근거: 발표자료 값(102.4 ▲1.2% · 98.7 ▼0.5% · 105.1 ▲0.8%, 66.7%)이 계산으로 다시 나오도록 역산했다. 통밀 09-15→09-16은 가이드 `SCENARIOS.up`(101.2 → 102.4)과 같다.

## MINOR-07 · 보합은 무효로 판정
- 결정: 확정값이 직전 확정값과 같으면 UP·DOWN 모두 정답이 아니므로 `VOID`(무효)로 처리해 분모에서 뺀다. 확정값이 없을 때(휴장·미공시)도 `VOID`.
- 근거: 발표자료 7장은 "직전 확정값 대비 상승/하락"과 "휴장·미공시 회차는 무효"만 정한다.
- 확인 필요: HUMAN_TODO TODO-26.

## MINOR-08 · 오늘 예측은 브라우저 localStorage에만 저장
- 결정: `/predict` 제출·수정 값은 `makji-mvp:prediction:2026-09-17` 키로 이 브라우저에만 저장한다. 저장소를 못 쓰면 메모리에만 두고, 읽을 때는 세 지수 모두 UP/DOWN인지 다시 검사한다.
- 근거: 이번 범위는 "DB·외부 시세 API를 쓰지 않는 목업"이라 서버 저장이 없다. 새로고침 후에도 저장 완료 화면과 월~금 도트를 보여 주려면 브라우저 저장이 필요하다.

---

## 종료 기록 (2026-09-15)

- 검증: `npm test` 56/56 · `npm run build` · `npm run lint` exit 0 · `node scripts/smoke.mjs` 24/24 PASS (aztks-agent 최종 판정에서 직접 재실행해 일치)
- 미검증(사람 작업 필요): 실제 카페24 OAuth·상품 16 수정/복원·Scripttags 설치, 실제 Supabase·Vercel 배포 — `docs/HUMAN_TODO.md`

STOP REASON: PROTOTYPE_VERIFIED
AZTKS VERDICT: GO

---

## 사후 추가 (2026-09-16, 사용자 요청 — Goal 1 종료 뒤, 카운터 미포함) · Supabase 준비 자동화

- POST-01 · **설치 스크립트** `npm run db:setup` / `npm run setup:check` (`scripts/db-setup.mjs`, 핵심 로직 `scripts/lib/db-setup-core.mjs`)
  - 마이그레이션은 `supabase/migrations/*.sql`을 번호 순으로 파일마다 트랜잭션으로 적용하고, 적용 기록(이름·sha256)을 API에 노출되지 않는 `lab_private.schema_migrations`에 둔다. 이미 적용한 파일의 내용이 바뀌면 멈춘다. 기존 SQL이 `if not exists`·`or replace`라서 SQL Editor로 이미 만든 DB에도 적용할 수 있다.
  - DB 접속은 `pg`(devDependency 8.23.0 고정)로 `SUPABASE_DB_URL`(Session pooler 문자열)을 쓴다. 이 값은 내 컴퓨터 `.env.local` 전용이며 Vercel에 넣지 않는다.
  - 스키마 확인: 테이블 3개 · RLS · 잠금 함수 · anon 권한 없음 · service_role 쓰기 권한.
- POST-02 · **관리자 계정 자동 생성**: `SETUP_ADMIN_EMAIL` 계정이 없을 때만 Supabase Auth Admin API로 이메일 확인된 계정을 만든다(있으면 절대 바꾸지 않음). 비밀번호는 파일에 두지 않고 터미널에서 두 번 입력(12자 이상)하거나 `--generate-password`로 만들어 한 번만 표시한다. `.env.local`의 `ADMIN_USER_IDS`가 비었거나 `pending`일 때만 UUID를 적고, 다른 UUID가 있으면 추가 제안만 한다. 웹 화면에서 관리자를 만드는 기능은 두지 않았다(인증 전 공개 화면에서 계정을 만들 수 있게 되면 선점 공격이 가능하므로).
- POST-03 · **준비 상태 점검 화면** `/admin/setup` + 규칙 `src/lib/setup/env-rules.ts`(순수 함수, 스크립트와 공용 — 스크립트는 Node 24 타입 제거로 .ts를 직접 불러온다) + 조회 `src/lib/setup/readiness.ts`(읽기 전용)
  - 접근: `ADMIN_USER_IDS`에 UUID가 설정되기 전(bootstrap)에는 로그인 없이, 설정 뒤에는 관리자만. 어느 경우든 값은 표시하지 않고 이름·상태·개수만 보여 준다.
  - 점검: 환경변수 8개 형식(키 자리 뒤바뀜, pending, 32바이트 키, APP_BASE_URL과 접속 주소 일치), 배포 서버에 `SUPABASE_DB_URL`이 있으면 경고, DB 테이블·잠금 함수(없는 몰 ID로 호출해 아무것도 바꾸지 않음)·Publishable key로 토큰 테이블 접근 불가, 관리자 UUID가 실제 확인된 계정인지, 카페24 연결 상태.
- POST-04 · **요청 시점 렌더링 강제**: `/admin/cafe24`·`/admin/setup`에서 `await connection()`. 환경변수 검사 뒤 redirect만 하고 cookies를 부르지 않는 경로가 생기자 빌드 때 정적 페이지(○)로 굳어 환경변수가 있어도 항상 `/admin/setup`으로 가는 문제가 smoke에서 드러났다.
- POST-05 · **설정 전 동작 변경**(가이드 계약 영향): `src/proxy.ts`는 Supabase 공개값이 없으면 세션 갱신을 건너뛰고, `/admin/cafe24`는 로그인에 필요한 값이 없으면 500 대신 `/admin/setup`으로 307. 값이 있을 때의 동작(로그인 없음 → `/login`, 비관리자 → UUID 안내)은 그대로다.
- POST-06 · **smoke 확장**: 가짜 Supabase에 Auth Admin(목록·생성·단건)·REST 권한 거부(42501)·잠금 RPC를 추가하고, 점검 화면(관리자 실패 0 · 로그인/비관리자 차단 · 비밀값 미노출)과 `db:setup` 전체 흐름(PGlite · 가짜 Auth · ADMIN_USER_IDS 기록 · 재실행 시 기존 계정 인식 · Secret 미출력) 4개 점검을 넣었다. 이 기능이 없는 프로젝트(학습자 프로젝트)는 SKIP으로 점수에서 빼므로 `--project` 기준은 24/24로 유지된다.
- 검증: npm test 10 files 82 passed · build · lint · smoke 28/28 · 설정 전(환경변수 없음) 서버에서 `/admin/cafe24` → `/admin/setup` 307, 점검 화면 200 · 기능을 뺀 복사본 smoke 24/24(SKIP 1줄) · `pg` 경로는 PGlite 소켓 서버(Postgres 와이어 프로토콜)로 적용→재실행 건너뜀→확인 모드 확인. 실제 Supabase(Session pooler·Auth Admin API)에는 접속하지 않았다.

---

## 사후 추가 (2026-09-16, 사용자 요청 — 실제 몰 운영 중 발견) · 수정 반영 확인 방식

- POST-07 · **수정 반영 확인: PUT 응답 우선, 없으면 재조회 재시도** (`src/lib/cafe24/product-sync.ts` `writeAndVerify`, `product-deps.ts` `putProduct`)
  - 발견: 실제 몰에서 판매가 적용·복원이 반영됐는데도 502 `WRITE_FAILED`(`VERIFY_MISMATCH`)가 났다. `cafe24_change_log`를 보면 PUT은 오류 없이 끝났지만 직후 조회가 이전 값을 돌려줬고, 이후 1~2분 동안 조회값이 4,940 ↔ 5,200으로 오락가락했다. 성공으로 인정되지 않아 `applied`가 저장되지 않았고, 이어진 복원이 `EDITED_ELSEWHERE`로 막히거나 `already`로 PUT을 건너뛰었다.
  - 결정: ① PUT이 성공하고 응답 `product`의 상품번호(상품코드가 있으면 상품코드도)가 맞고 이번에 바꾼 필드가 목표값과 같으면 그것으로 확인(`verifiedBy: "put_response"`). 바꾸지 않은 필드가 응답에 없으면 수정 전 값으로 본다. ② 응답으로 확인하지 못하면 0·1·2·3·4초 간격으로 최대 5번 다시 조회해 목표값이 한 번이라도 보이면 확인(`verifiedBy: "reread"`, `readAttempts`). 카페24가 4xx로 거절한 PUT은 기다리지 않고 한 번만 조회한다. ③ 끝까지 확인하지 못했을 때의 결과 코드(`WRITE_FAILED` 502 / `WRITE_UNKNOWN` 500)는 그대로 두고 `detail.readAttempts`를 더했다.
  - 근거: 목표값은 수정 전 값과 다르므로 조회에서 목표값이 한 번이라도 보였다면 쓰기가 반영된 것이다. 카페24 문서에서 PUT 응답 형식을 확인하지 못해 응답에 값이 올 때만 쓰고, 없으면 기존 재조회 경로로 돌아간다.
  - 남은 한계: 적용 직후 곧바로 [상품 조회]·[복원]·두 번째 [적용]을 누르면 조회가 이전 값을 줄 수 있어 `already`·`EDITED_ELSEWHERE` 판단은 여전히 틀릴 수 있다(가이드 G 단계에 "1~2분 뒤" 안내). 교육가이드(AI-Native v3)의 에이전트 프롬프트·계약에는 아직 반영하지 않았다.
- 검증: npm test 10 files 89 passed(가짜 몰에 "PUT 뒤 N번 이전 값 조회"·PUT 응답 전체/일부/다른 상품 사례 7개 추가) · tsc · lint · build · smoke 28/28. 실제 카페24 몰에는 에이전트가 요청하지 않았다.

- POST-08 · **쓰기 없이 끝나는 판단(이미 같음·충돌) 보강** (같은 파일 `applyChange`·`restoreBaseline`)
  - 발견: POST-07 뒤 적용은 매끄러워졌지만 복원이 막혔다. 이력 #8 적용(4,940) 직후 #9 복원에서 조회가 5,200(이전 값)을 돌려줘 "이미 기준값"으로 끝났고, 이때 `applied`가 null로 지워졌다. 이후 #10~#13 복원은 실제값 4,940과 대조할 기록이 없어 모두 `EDITED_ELSEWHERE` 충돌이 됐다. PUT 확인만 고쳐서는 쓰기 전 조회가 만드는 오판을 막지 못한다.
  - 결정: ① 저장 상태와 어긋나는 "이미 같음" 판단 앞에서 1·2·3초 간격으로 최대 3번 다시 조회한다(`RECHECK_DELAYS_MS`). 복원은 `applied`가 남아 있는데 기준값으로 읽힐 때, 적용은 적용 기록이 없는데 목표값으로 읽힐 때만 확인하므로, 같은 적용을 반복하는 정상 경로는 그대로 빠르다. ② 복원 충돌 판정을 "마지막 적용값과 다름"에서 "이 앱이 만들 수 있는 값(시나리오 × 할인 여부)이 아님"으로 바꿨다. 적용 기록이 지워져도 우리가 쓴 값이면 복원할 수 있고, 사람이 직접 넣은 값(4,500 등)은 여전히 409로 멈춘다.
  - 근거: 조회가 흔들리는 동안에도 "우리가 쓸 수 있는 값의 집합"은 기준값에서 계산되므로 흔들리지 않는다. 사람이 바꾼 값을 덮어쓰지 않는다는 원래 안전장치는 유지된다.
  - 남은 한계: 사람이 우연히 앱과 똑같은 값(4,940원 + 같은 요약설명)을 직접 넣으면 앱이 쓴 값으로 보고 복원한다. [상품 조회]가 보여 주는 현재값은 카페24 조회 그대로라 수정 직후 1~2분은 이전 값일 수 있다.
- 검증: npm test 10 files 95 passed(복원 오판·적용 기록 소실·사람 수정 구분 등 6개 추가, 새 테스트 4개는 이전 코드에서 실패함을 확인) · tsc · lint · build · smoke 28/28.
